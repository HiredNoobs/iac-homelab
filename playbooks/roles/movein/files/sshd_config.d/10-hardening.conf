# Managed by Ansible. sshd keeps the first value it reads for each option, this is
# included before 50-cloud-init.conf so it wins.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitEmptyPasswords no
AuthenticationMethods publickey
X11Forwarding no
