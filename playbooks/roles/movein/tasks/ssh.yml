# Each host gets its own keypair, which is authorized on every other host in the
# same group so they can jump between each other.
- name: Generate SSH keypair
  user:
    name: "{{ movein_username }}"
    generate_ssh_key: yes
    ssh_key_type: ed25519
    ssh_key_file: ".ssh/id_ed25519"
  register: movein_ssh_key

- name: Authorize keys between hosts
  authorized_key:
    user: "{{ movein_username }}"
    key: "{{ hostvars[item].movein_ssh_key.ssh_public_key }}"
    state: present
  loop: "{{ ansible_play_hosts }}"
  when: item != inventory_hostname

# Key only, Ansible connects as movein_username with the key cloud-init authorized.
- name: Harden sshd
  copy:
    src: sshd_config.d/10-hardening.conf
    dest: /etc/ssh/sshd_config.d/10-hardening.conf
    mode: '0644'
    validate: /usr/sbin/sshd -t -f %s
  notify: reload sshd
